{% extends 'admin/base.html.twig' %}

{% block title %}Ajout Recette{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-5 text-center" style="background:#32a86d; border-radius:5px; color:white;">   
                <h4>Modification recette numéro : {{inco.id}}</h4>
            </div>         
        </div>
        <br>
        <div>
            <div class="col-md-8">
                {{form_start(form)}}
                {{form_widget(form)}}
                <button type="submit" class="btn btn-success">Enregister modifications</button>
                {{form_end(form)}}
            </div>  
        </div>   
    </div>
{% endblock %}