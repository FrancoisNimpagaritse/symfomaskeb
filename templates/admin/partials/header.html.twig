<nav class="navbar navbar-default">
    <a class="navbar-brand" href="{{ path('homepage')}}">M<strong>askeb</strong></a>
    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
    </button>
    <div id="navbar" class="collapse navbar-collapse">
        <ul class="nav navbar-nav mr-auto">
            <li class="nav-item">
                <a href="{{ path('admin_dashboard') }}" class="nav-link"><i class="glyphicon glyphicon-signal"></i> Dashboard</a>
            </li>
            <li class="nav-item dropdown">
                <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="catDropdownLink"><i class="glyphicon glyphicon-cog"></i>
                    Paramètres
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="catDropdownLink">
                    <a href="{{ path('admin_categoryincome_index') }}" class="dropdown-item"> Catégories ressources</a>
                    <div class="divider"></div>
                    <a href="{{ path('admin_categoryexpense_index')}}" class="dropdown-item"> Rubriques dépenses</a>
                    <div class="divider"></div>
                    <a href="#" class="dropdown-item"> Utilisateurs</a>
                    <div class="divider"></div>
                    <a href="{{ path('admin_donors_index') }}" class="dropdown-item"> Donateurs</a>
                    <div class="divider"></div>
                    <a href="{{ path('admin_beneficiaries_index') }}" class="dropdown-item"> Enfants</a>
                </div>
            </li>
            <li class="nav-item">
                <a href="{{ path('admin_incomes_index') }}" class="nav-link"><i class="glyphicon glyphicon-plus-sign"></i> Recettes</a>
            </li>
            <li class="nav-item">
                <a href="{{ path('admin_expenses_index') }}" class="nav-link"><i class="glyphicon glyphicon-minus-sign"></i> Dépenses</a>
            </li>            
        </ul>
        <ul class="nav navbar-nav navbar-right ml-auto">
                <li class="nav-item">
                <a href="{{ path('homepage') }}" target="_blank" class="nav-link"><i class="glyphicon glyphicon-eye-open"></i> Voir le site</a>
            </li>
            {% if app.user %}
            <li class="nav-item dropdown">
                <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink"><i class="glyphicon glyphicon-user"></i>
                    <img src="" alt="">{{ app.user.firstname }}
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                    <a href="{{ path('account_index') }}" class="dropdown-item"> Mon compte</a>
                            <div class="divider"></div>
                    <a href="{{ path('account_profile') }}" class="dropdown-item"> Modifier mon profil</a>
                            <div class="divider"></div>
                    <a href="{{ path('account_logout') }}" class="dropdown-item"> Déconnexion</a>                   
                </div>
            </li>            
            {% endif %}
        </ul>   
    </div>     
</nav>
<header id="header">
    <div class="container">
        <div class="row">
            <div class="col-md-10">
                <h1 class="text-center"><small>Maskeb - </small> {{ bodyTitle }}</h1>
            </div>                    
        </div>
    </div>
</header>